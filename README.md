# LCDPanel
STM32F411 project for controlling a 16×2 HD44780-compatible character LCD through an 8-bit parallel GPIO interface. It provides a C driver for initializing the display, positioning the cursor, and displaying characters and text, with a simple example demonstrating its use.
